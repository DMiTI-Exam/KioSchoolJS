import{Graph}from"../../kioschool/basic/model/Graph.js";export class PrimGraph extends Graph{constructor(){super()}getBoundaryEdges(){let boundaryEdges=[];for(let edge of this.edgesGetter()){if(edge.sourceGetter().inTreeGetter()&&!edge.targetGetter().inTreeGetter()||edge.targetGetter().inTreeGetter()&&!edge.sourceGetter().inTreeGetter()){boundaryEdges.push(edge)}}return boundaryEdges}getAmountOfVertexesInTree(){let sum=0;for(let vertex of this.vertexesGetter()){if(vertex.inTreeGetter()){sum+=1}}return sum}getMinimumFromSelectedEdges(){let edge;for(let e of this.edgesGetter()){if(e.selectedGetter()&&(edge==null||e.weightGetter()<edge.weightGetter())){edge=e}}return edge}}